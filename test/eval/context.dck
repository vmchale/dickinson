(:def letBinding
    (:let
        [a "woman"]
        (:branch
            (| 1.0 a)
            (| 1.0 a))))

(:def main
    (:let
        [a letBinding]
        (:branch
            (| 1.0 letBinding)
            (| 1.0 a))))
